---
export interface Props {
	class?: string;
	style?: string | Record<string, string>;
}

const { class: className, style } = Astro.props;
---
<div class={`relative px-4 sm:px-8 lg:px-12 ${className}`} style={style}>
	<div class="mx-auto max-w-2xl lg:max-w-5xl">
		<slot />
	</div>
</div>