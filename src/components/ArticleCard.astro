---
import Card from "./Card.astro";

export interface Props {
	href: string;
	title: string;
	date: string;
}

const { href, title, date } = Astro.props;
---
<Card as="article" href={href} title={title} date={date} cta="Read article">
	<slot />
</Card>